const add = (a, b) => a + b;
const subtract = (a, b) => a - b;

// module.exports = {
//     add,
//     subtract
// };

// exports.add1 = add;
// exports.subtract1 = subtract;

export default { add, subtract };